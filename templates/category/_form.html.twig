<div class="container-lg">
    {{ form_start(form) }}
    {{ form_row(form.name, {label: 'Name', attr: {class: 'form-control'}}) }}
    {{ form_row(form.articles, {label: 'Articles', attr: {class: 'form-control checkbox-list', placeholder: 'Articles ?'}}) }}

    <div class="d-grid">
        <button type="submit" class="btn btn-primary">{{ submit_label }}</button>
    </div>
    {{ form_end(form) }}
</div>
